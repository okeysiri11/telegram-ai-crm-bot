# AI Flow Builder

Generate, optimize, and document workflows from natural-language prompts.

## API

`POST /api/workflow-studio/v1/ai-builder`

Actions: `generate`, `optimize`, `suggest`, `bottlenecks`, `cost`, `docs`.
