# Compliance

Compliance Center frameworks, policy enforcement, audit trail integration for Enterprise Edition.

## API

`POST /api/enterprise/v1/administration` with `action=compliance|policy|audit`
