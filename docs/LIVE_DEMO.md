# Live Demo

**Sprint:** 30.6  
**Route:** `/demo/scenario`  
**Catalog:** `platform-integration/betaLiveDemo.ts`

## Beta walkthrough (8–15 min)

1. **Login** — `/login`  
2. **Dashboard** — `/dashboard`  
3. **City** — `/city`  
4. **Open Building** — `/enterprise-city?building=crm`  
5. **Launch AI Agent** — `/ai-agents`  
6. **Production Studio** — `/production-studio`  
7. **Generate task** — `/production-studio?studio=image` (Создать изображение)  
8. **Return result** — `/production-studio?tab=history`  

## Pitch (RU)

Вход → Дашборд → Город → Здание → AI-агент → Продакшн → Задача → Результат — одна платформа.

Extended executive path remains in `demoScenarioCatalog.ts` on the same page.
