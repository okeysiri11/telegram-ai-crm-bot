# Global Command

Operations Center with global dashboard, fleet/mission maps, live aircraft tracking, operator dashboard, incident dashboard, and mission timeline.

## API

- `GET|POST /api/drone/v1/cloud/command`

AI Command Center extends Mission AI with fleet monitoring, failure/maintenance prediction, operator recommendations, and anomaly detection.
