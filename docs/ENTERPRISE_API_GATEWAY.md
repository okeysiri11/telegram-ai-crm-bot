# Enterprise API Gateway

**Version:** `5.3.0-enterprise`  
**API:** `POST /api/enterprise-hub/v1/integration` with `action: gateway|route|discover|aggregate|bus`

Gateway accepts routed requests, discovers registered services, and aggregates multi-platform responses.
