# Project Memory

**Epic:** 45.2 · Level 3

История проекта, документы, решения, версии, заметки.

Continuous layer: `memory_manager.project` + `WorkingMemory.upsert_project`.  
Canonical engine (36.5): `project_memory_engine` / `project_memory_service` — reused, not duplicated.
