# Enterprise APIs

REST, GraphQL, WebSocket, MQTT, MAVLink, ROS2, Mission Planner, and QGroundControl bridges for enterprise cloud integration.

## API

- `GET|POST /api/drone/v1/cloud/enterprise`

Security: encrypted telemetry/commands, certificates, RBAC, operator/mission permissions, audit logs via `/api/drone/v1/cloud/security`.
