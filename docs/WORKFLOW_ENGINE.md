# Workflow Engine

**Epic 45.3** · Universal Automation · `platform_workflows`

Façade: `workflow_manager` · Runtime: `UniversalWorkflowEngine` (`ua_engine.py`).
Legacy YAML engine preserved. Canonical graph SoR remains `platform_workflow`.

All AI steps execute **via Hercules only**.
