# ESH — Identity, RBAC & ABAC

Authentication methods: OAuth2, OIDC, JWT, refresh tokens, service accounts, API keys, MFA. Authorization combines role checks with attribute policies (organization, department, project, owner, secrecy, geo, operation).
