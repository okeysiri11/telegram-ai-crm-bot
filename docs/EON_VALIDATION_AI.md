# EON — Validation Engine & AI Migration Assistant

Validation Engine checks duplicates, structure errors, missing fields, conflicts, and formats; returns a report with suggested fixes.

AI Migration Assistant maps columns, detects errors, proposes fixes, scores data quality, and warns about risks. AI never changes import data automatically.
