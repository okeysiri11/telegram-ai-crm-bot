# Recovery

Automatic return, safe landing, mission resume, connection recovery, sensor reconfiguration, emergency flight modes, and recovery reports.

## API

- `GET|POST /api/drone/v1/resilience/recovery`
