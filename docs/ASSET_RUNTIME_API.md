# Asset Runtime API

**Sprint:** 29.3  
**Prefix:** `/api/enterprise-assets/v1`

| Method | Path | Purpose |
|--------|------|---------|
| GET | `/health` | Health |
| GET | `/assets` | List assets |
| GET | `/assets/:id` | Asset detail |
| GET | `/city` | City inventory query |
| GET | `/inventory` | Endpoints + stats |

In-process: `assetRuntime` / `assetRuntimeApi` (CRUD · assign · transfer · move · lifecycle).
