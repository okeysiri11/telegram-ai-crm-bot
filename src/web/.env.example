# Local frontend (Vite)
VITE_DEMO_AUTH=true
VITE_DEV_HOST=127.0.0.1
PORT=5180
# Optional local override of the existing DEV Owner password. Leave unset to use the built-in DEV default.
# VITE_DEMO_OWNER_PASSWORD=
