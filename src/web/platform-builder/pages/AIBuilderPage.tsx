import { AIBuilderStudioPage } from "@/ai-builder-studio";

/** AI Builder entry — Sprint 32.8 Studio (wizard available via ?mode=wizard). */
export function AIBuilderPage() {
  return <AIBuilderStudioPage />;
}
