import { Navigate } from "react-router-dom";

/** Sprint 17 — entrance lives in CasinoApp. Keep export for older imports. */
export function CasinoLobbyPage() {
  return <Navigate to="/casino" replace />;
}
